spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/myclubmanager
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    database: postgresql
    hibernate:
      ddl-auto: update
    show-sql: false

application:
  mailing:
    activation-url: http://localhost:4200/activate-account